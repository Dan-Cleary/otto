{
  "name": "otto",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "widget:build": "esbuild widget/src/index.ts --bundle --minify --outfile=app/public/otto.js --target=es2020 --loader:.svg=base64",
    "test": "vitest run"
  },
  "dependencies": {
    "@auth/core": "^0.37.0",
    "@convex-dev/auth": "^0.0.92",
    "@cursor/sdk": "^1.0.10",
    "convex": "^1.17.0",
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^22.0.0",
    "convex-test": "^0.0.50",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
